version: '3.8'

services:
  app:
    build: .
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
      - JWT_SECRET=${JWT_SECRET}
      - SITE_NAME=${SITE_NAME:-论文助手}
    volumes:
      - ./server/data:/app/server/data
    restart: unless-stopped